{
  "name": "@rifrocket/fdt-theme",
  "version": "0.0.0",
  "private": true,
  "description": "CSS design tokens and light/dark theme for the FabricJS Design Tool v2 (preview scaffold — not yet published).",
  "type": "module",
  "style": "./src/tokens.css",
  "files": [
    "src"
  ],
  "scripts": {
    "build": "echo \"no build step — tokens.css is consumed directly from src/\"",
    "test": "echo \"no tests — pure CSS package\"",
    "lint": "echo \"no lint configured yet\""
  }
}
