# @rifrocket/fdt-theme

CSS custom-property design tokens for light/dark theming, with zero JS runtime cost.

**Status:** scaffold only, not published. `packages/react` components will consume `var(--fdt-*)` from `src/tokens.css`; Tailwind is not used in any published v2 package.
