# @rifrocket/fdt-core

Framework-agnostic canvas engine for the FabricJS Design Tool v2 redesign.

**Status:** scaffold only, not published.

`fabric` is a peer dependency; this package must never depend on React or any other UI framework — that boundary is enforced by not listing them as dependencies here.
