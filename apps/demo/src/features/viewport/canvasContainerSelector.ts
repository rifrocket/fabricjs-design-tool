// Shared by every consumer of @rifrocket/fdt-plugin-pan-zoom's selector-based hooks/helpers
// (useCanvasPanZoom, useContainerSize, centerContent) and CanvasWorkspace.tsx, which renders the
// element this selector must match.
export const CANVAS_CONTAINER_SELECTOR = '[data-fdt-canvas-container="true"]';
